package com.example.hearmeout.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Main application color palette.
 * Colors are chosen for high contrast and modern aesthetics,
 * specifically tailored for accessibility-focused UI.
 */

// Primary background color for the main screens
val DarkBlue = Color(0xFF13174D)

// Secondary color used for headers and UI elements
val Turquoise = Color(0xFF406882)

// High-visibility color for safety alerts and danger notifications
val SafetyOrange = Color(0xFFFF8E47)

// Deep navy used for the root background to ensure text readability
val BackgroundNavy = Color(0xFF0D102D)

// Standard high-contrast text color
val WhiteText = Color(0xFFFFFFFF)